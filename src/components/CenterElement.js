import { Box } from "@mui/material";
import { styled } from "@mui/system";

const CenterElement = styled(Box)({
    display: "flex",
    justifyContent: "center",
    alignItems: "center"
});

export default CenterElement;
